spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tsl
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/master.xml
  mail:
    host: smtp.gmail.com
    port: 587
    username: testtslapp@gmail.com
    password: msjt kgoc ypxq ahol
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
